class Solution:
    def percentageLetter(self, s: str, letter: str) -> int:
        n = len(s)
        (n.bit_length().bit_length())
        print(n)
        return round(math.floor((s.count(letter)/len(s))*100))     

